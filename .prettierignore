node_modules
.expo
dist
release
storybook-static
electron/dist
pnpm-lock.yaml
*.ico
*.png
*.jpg
*.svg

# Hand-authored low-fi wireframes — intentionally not prettier-formatted
docs/**/*.html
scripts/poc-embedder/

# Vendored agent skills — kept verbatim, not project source (matches .remarkignore)
.agents/

# Symlink to CLAUDE.md — prettier errors on symbolic links; the target gets formatted instead.
AGENTS.md

# drizzle-kit output — meta/ JSON is checksummed (prettier desyncs it) and .sql
# carries statement-breakpoint markers; the migrations.js bundle is plain JS, safe
# to format.
lib/db/migrations/meta/
lib/db/migrations/**/*.sql
